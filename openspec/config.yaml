# SDD Configuration — stock-app
# Auto-generated by sdd-init on 2026-07-04

context: |
  Java 21 + Spring Boot 4.1.0 Maven project (Thymeleaf + PostgreSQL + Lombok).
  Product/order/linea management app with admin auth, shopping cart, and inventory movements.
  Stack: spring-boot-starter-web, spring-boot-starter-jdbc, spring-boot-starter-thymeleaf,
  spring-boot-starter-test (JUnit 5 + Mockito + AssertJ), PostgreSQL 42.7.2, Lombok.
  No CI workflows. Maven wrapper (mvnw.cmd) for Windows.

strict_tdd: true

testing:
  runner:
    command: mvnw.cmd test
    framework: JUnit 5 (spring-boot-starter-test)
  layers:
    unit: { available: true, tool: "JUnit 5 + Mockito" }
    integration: { available: true, tool: "@SpringBootTest" }
    e2e: { available: false, tool: null }
  coverage:
    available: false
    command: null
  quality:
    linter: { available: false, command: null }
    type_checker: { available: false, command: null }
    formatter: { available: false, command: null }

phases:
  proposal: { rules: "Intent + scope + approach. Max 1-page." }
  spec: { rules: "Delta specs with requirements and scenarios." }
  design: { rules: "Technical design and architecture approach." }
  tasks: { rules: "Break change into implementation tasks." }
  apply: { rules: "Implement from tasks. No skipping." }
  verify: { rules: "Tests must pass. Prove spec compliance." }
  archive: { rules: "Sync delta specs after completion." }
